
def keskmine(nr):

        total = sum(nr) / len(nr)
        
        print (total)

print "----------------"

def fibo(n):
    a, b = 0, 1
    for i in xrange(n):
        yield a
        a, b = b, a + b

d = int(raw_input("Mitu numbrit?"))
for i in fibo(d):
    print i,


print "/n----------------"
